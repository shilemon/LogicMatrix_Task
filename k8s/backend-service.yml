apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
spec:
  type: ClusterIP   # internal only - never exposed directly to the internet
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
